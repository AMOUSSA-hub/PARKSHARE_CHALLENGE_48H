services:
  # --- 1. LE REVERSE PROXY ---
  proxy:
    image: nginx:alpine
    container_name: parkshare-proxy
    ports:
      - "80:80"  
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - parkshare_net
    depends_on:
      - frontend
      - api
    restart: unless-stopped

  # --- 2. LA BASE DE DONNÉES ---
  db:
    image: postgres:15-alpine
    container_name: parkshare-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - parkshare_net
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  # --- 3. LE BACKEND (API) ---
  api:
    build: 
      context: ../app
    container_name: parkshare-api
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      # On couvre tous les noms de variables classiques utilisés par les Devs
      POSTGRES_HOST: db
      DB_HOST: db
      PGHOST: db
      HOST: db
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    networks:
      - parkshare_net
    depends_on:
      - db
    restart: unless-stopped

  # --- 4. LE FRONTEND (React) ---
  frontend:
    build: 
      context: ../app/frontend
    container_name: parkshare-frontend
    environment:
      # Chemin relatif : indique au navigateur de chercher l'API sur le même serveur que le site
      VITE_API_URL: /api
    networks:
      - parkshare_net
    depends_on:
      - api
    restart: unless-stopped

# --- RÉSEAU ET VOLUMES ---
networks:
  parkshare_net:
    driver: bridge

volumes:
  pgdata: